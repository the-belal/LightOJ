#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define mx 105
#define __faster ios_base::sync_with_stdio(false); cin.tie(NULL);
bool a[mx];
vector<ll>v;
ll sieve()
{
    ll r,i,j;
    for (i=4;i<mx;i+=2)
    {
        a[i]=true;
    }
    r=sqrt(mx)
    for (i=3;i<)
}
int main ()
{
    __faster;
    ll tc,t,i,j,n,m,cnt;
    cin>>tc;
    for (t=1;t<=tc;t++)
    {
        cin>>n;
    }

}
